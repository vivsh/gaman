{
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:wasm": "bash scripts/build-wasm.sh",
    "dev": "npm run prepare:wasm && vite",
    "build": "npm run prepare:wasm && vite build"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^6.0.0"
  }
}
