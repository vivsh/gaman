description: MariaDB rejects unsupported SET DEFAULT foreign key actions during preparation
dialect: mariadb
sql: |
  CREATE TABLE parent (id INT PRIMARY KEY);
  CREATE TABLE child (parent_id INT, FOREIGN KEY (parent_id) REFERENCES parent(id) ON UPDATE SET DEFAULT);
expect_error: SET DEFAULT
