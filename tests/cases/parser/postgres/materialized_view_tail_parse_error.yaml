---
description: PostgreSQL materialized views are rejected outside EntityKind.
dialect: postgres
sql: |
  CREATE MATERIALIZED VIEW active_users AS SELECT id FROM users WITH DATA;
expect_error: known Gaman entity kind
