---
description: PostgreSQL materialized view WITH DATA tail is reported as parser error.
dialect: postgres
sql: |
  CREATE MATERIALIZED VIEW active_users AS SELECT id FROM users WITH DATA;
expect_error: SQL parse error
