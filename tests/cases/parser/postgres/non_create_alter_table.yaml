description: PostgreSQL ALTER TABLE is rejected because parser input is CREATE-only.
dialect: postgres
sql: |
  ALTER TABLE users ADD COLUMN email text;

expect_error: only CREATE statements for modeled schema entities are parsed
