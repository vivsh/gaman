description: PostgreSQL parser accepts policies but Gaman does not model them.
kind: sql_parse
parser_dialect: postgres
sql: |
  CREATE POLICY tenant_isolation
  ON invoices
  USING (tenant_id = current_setting('app.tenant_id')::uuid);
expect_parse: ok
expect_lowering: unsupported
expect_error: unsupported
