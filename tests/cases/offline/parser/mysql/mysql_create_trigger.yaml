---
description: MySQL trigger lowering is unsupported while MySQL remains a stub dialect.
group: parser
features:
- group.parser
- kind.parser
- parser.mysql.parser_mysql_create_trigger
kind: parser
parser_dialect: mysql
sql: |
  CREATE TRIGGER users_bi
  BEFORE INSERT ON users
  FOR EACH ROW SET NEW.created_at = NOW();
expect_parse: error
expect_lowering: error
expect_error: unsupported SQL dialect
