---
description: MySQL prefix indexes are accepted as opaque owned indexes.
group: parser
features:
- group.parser
- kind.parser
- parser.mysql.parser_mysql_create_prefix_index
kind: parser
parser_dialect: mysql
sql: |
  CREATE TABLE users (email varchar(255));
  CREATE INDEX users_email_prefix_idx ON users (email(32));
expect_parse: ok
expect_lowering: ok
