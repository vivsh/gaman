---
description: MySQL composite-key lowering is unsupported while MySQL remains a stub dialect.
group: parser
features:
- group.parser
- kind.parser
- parser.mysql.parser_mysql_create_table_composite_fk
kind: parser
parser_dialect: mysql
sql: |
  CREATE TABLE accounts (
      tenant_id int,
      region_id int,
      PRIMARY KEY (tenant_id, region_id),
      FOREIGN KEY (tenant_id, region_id) REFERENCES tenants (tenant_id, region_id)
  ) ENGINE=InnoDB;
expect_parse: error
expect_lowering: error
expect_error: unsupported SQL dialect
