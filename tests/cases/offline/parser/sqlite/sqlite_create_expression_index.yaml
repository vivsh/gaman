---
description: SQLite parser lowers expression indexes into table index metadata.
group: parser
features:
- group.parser
- kind.parser
- parser.sqlite.parser_sqlite_create_expression_index
kind: parser
parser_dialect: sqlite
sql: |
  CREATE TABLE users (email text);
  CREATE INDEX users_lower_email_idx ON users (lower(email)) WHERE email IS NOT NULL;
expect_parse: ok
expect_lowering: ok
