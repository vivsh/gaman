---
description: RenameNo for an enum value leaves enum replacement operations unchanged.
group: disambiguator
features:
- answer.rename_no
- group.disambiguator
- kind.schema_to_migration
kind: schema_to_migration
dialect: postgres
name: replace_status_label
migrations:
- id: 0001_create_status
  operations:
  - type: create_enum
    enum_def:
      name: status
      values:
      - active
      - inactive
decisions:
- clarification_id: rename_enum_value:status:inactive
  answer:
    type: rename_no
current:
  tables: {}
  enums:
    status:
      name: status
      values:
      - active
      - disabled
expect_operations:
- type: create_enum
  enum_def:
    name: status
    values:
    - active
    - disabled
- type: drop_enum
  enum_def:
    name: status
    values:
    - active
    - inactive
