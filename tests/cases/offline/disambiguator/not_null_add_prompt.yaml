---
description: Adding a non-null column without a default emits a structured not-null-add
  clarification.
group: disambiguator
features:
- clarification.not_null_add
- group.disambiguator
- kind.schema_to_migration
kind: schema_to_migration
dialect: postgres
name: add_status
migrations:
- id: 0001_create_users
  operations:
  - type: create_table
    table:
      name: users
      columns:
      - name: id
        type: integer
        primary_key: true
current:
  tables:
    users:
      name: users
      columns:
      - name: id
        type: integer
        primary_key: true
      - name: status
        type: text
        nullable: false
expect_clarifications:
- id: notnull_add:users:status
  severity: fatal
  kind:
    type: not_null_add
    table: users
    column: status
    col_type: text
