---
description: End-to-end offline planning generates, replays, and renders a migration.
group: end_to_end
features:
- end_to_end.end_to_end_add_email
- group.end_to_end
- kind.end_to_end
- renderer.postgres.add_column
- renderer.postgres.create_table
kind: end_to_end
dialect: postgres
name: add_email
migrations:
- id: 0001_create_users
  operations:
  - type: create_table
    table:
      name: users
      columns:
      - name: id
        type: integer
        primary_key: true
current:
  tables:
    users:
      name: users
      columns:
      - name: id
        type: integer
        primary_key: true
      - name: email
        type: text
        nullable: true
expect_operations:
- type: add_column
  table_name: users
  column:
    name: email
    type: text
    nullable: true
expect_schema:
  tables:
    users:
      name: users
      columns:
      - name: id
        type: integer
        primary_key: true
      - name: email
        type: text
        nullable: true
expect_sql: 'ALTER TABLE "users" ADD COLUMN "email" text

'
