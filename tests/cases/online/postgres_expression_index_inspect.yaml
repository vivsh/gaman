description: PostgreSQL inspect preserves expression index metadata without failing
features:
  - live_inspect_verify
  - indexes
  - opaque_index_metadata

setup_sql: |
  CREATE TABLE users (
      id integer PRIMARY KEY,
      email text NOT NULL
  );
  CREATE INDEX users_lower_email_idx ON users ((lower(email)));

dialects:
  postgres:
    checks: [inspect]
    expect_schema:
      tables:
        users:
          name: users
          primary_key:
            name: users_pkey
            columns: [id]
          columns:
            - name: id
              type: integer
              primary_key: true
            - name: email
              type: text
          indexes:
            - name: users_lower_email_idx
              columns: []
              opaque:
                raw: CREATE INDEX users_lower_email_idx ON users USING btree (lower(email))
                trusted: true
                fingerprint: v1:sha256:e780c8e83186a869de6f3403856b80c07c561b05de6104857913933095a741d8
