description: Inspect a SQLite schema created with setup SQL.
kind: inspect
setup_sql: |
  CREATE TABLE users (
      id INTEGER PRIMARY KEY,
      email TEXT NOT NULL
  );
expect_schema:
  tables:
    users:
      name: users
      columns:
        - name: id
          type: integer
          primary_key: true
        - name: email
          type: text
